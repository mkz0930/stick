# ChatOverlay 推荐主题 prompt 调优报告

## 目标
解决用户反馈"出现执行某个动作这种建议"的问题。修改 ChatOverlay 的推荐主题 prompt，引导 LLM 输出"用户会继续追问的具体细节"，而非"AI 给的行动建议"。

## 测试方法
- 每次跑 10 次（用同一个 prompt 调 LLM 10 次）
- 检查输出是否含"试试/起身/喝杯/做一组/建议/不妨"等动作或建议词
- 6 轮共 60 次 API 调用, **0/60 action suggestion**

## 关键改动 (ChatOverlay.swift line 996-1037)

### 1. 删"立刻执行"方向 (原 line 1006)
原 prompt 有 3 类方向 (追问细节 / **立刻执行** / 继续探索), 删"立刻执行" — 这是诱导 LLM 输出 action suggestion 的元凶。

### 2. 加【不要做】块
```
【不要做】
- 不要给"行动建议"!用户不缺行动建议,AI 上一条已经给了。
  不要输出"试试 X"、"喝杯 X"、"起身 X"、"做 X 分钟"这种用户会去执行的动作
- 不要输出抽象话题标签(如"健康"、"睡眠"),要具体
- 不要问号结尾的疑问句(用陈述句口吻)
```

### 3. 加【风格示例】❌ 不好
列出反例 (LLM 看到反例会避免):
```
【风格示例】(❌ 不好 — 这种是 action suggestion, 严禁)
- 站起来走一走
- 试试喝杯咖啡
- 做一组深蹲
```

### 4. 强化"用户会主动输入"定位
开头 + 输出格式都强调"模拟用户会打的字", 不是"AI 给建议"。

## 6 轮 60 次 API 测试结果

### Round 0: Baseline 测现有 prompt
- **input**: "下午很困"
- **AI reply**: 困倦可能与血糖、CO₂、缺乏运动有关, 建议起身活动...
- **prompt**: 现有版本 (3 类方向, "严禁建议" 过滤)
- **结果**: 10/10 全是用户会问的问题 (血糖怎么测/晒太阳避紫外线/睡眠质量等), **无 action suggestion**
- **结论**: baseline 已 OK, 但 prompt 显式写"立刻执行"埋雷, Round 1 删它

### Round 1: 删"立刻执行" + 加"不要行动建议"
- **input**: "下午很困"
- **AI reply (action-heavy)**: 起身到窗边站 5 分钟, 做 20 个深蹲, 喝浓茶, 跑步 3 次...
- **prompt**: 删 line 1006 立刻执行方向, 加【不要做】块
- **结果**: 10/10 全是用户会问的细节
  - "美式咖啡每天最多喝几杯"
  - "深蹲做20个会不会伤膝盖"
  - "浓茶喝哪种茶叶"
- **结论**: 改后 prompt 抗住 action-heavy reply

### Round 2: 最强 action 诱导 AI reply
- **input**: "下午很困"
- **AI reply (最强诱导)**: 建议你马上喝浓缩咖啡. 建议你起身去窗边站 5 分钟. 建议你做 10 个深蹲. 试一下这些方法吧.
- **prompt**: 同 Round 1 (不修改, 测稳定性)
- **结果**: 10/10 全是问句 (咖啡喝多了会心慌吗/深蹲做不对伤膝盖吗/窗边站5分钟要晒太阳吗)
- **结论**: 即使 AI reply 满屏"建议你", prompt 仍能引导 LLM 输出**用户会问的细节**

### Round 3: 3 个不同 input × 3-4 次 (覆盖不同 user 痛点)
- **input1**: 睡不好 (3 次)
  - 刷手机影响哪里/温牛奶多少ml/泡脚水温
- **input2**: 运动伤膝 (3 次)
  - 前脚掌着地怎么练/膝盖内扣怎么自测/跑量 10% 按周还是月
- **input3**: 血压偏高 (4 次)
  - 盐 5g 多少勺/有氧选快走还是游泳/深呼吸怎么操作
- **结果**: 10/10 全是用户会问的细节
- **结论**: prompt 在不同 user 痛点下都稳定

### Round 4: edge case (4+3+3=10 runs)
- **极短 AI reply** ("你可能需要休息。" 1 句) → "休息多久/缺觉/午睡影响晚上吗"
- **极长 AI reply** (309 字 + 5 个"建议你") → "血糖怎么测/深呼吸怎么做/绿茶咖啡区别"
- **满屏祈使 reply** ("起身走一走/喝杯咖啡/做深蹲/深呼吸/晒太阳") → **反向**输出"咖啡喝多了会怎样/深蹲做多少个/晒太阳多久"
- **结果**: 10/10 clean

### Round 5: medical claims (3+3+4=10 runs)
- **胸闷** (AI: 立即拨打 120, 立刻舌下含服硝酸甘油) → "硝酸甘油怎么含服/心梗和心绞痛区别/肺栓塞症状"
- **头痛** (AI: 立即做 CT+MRI+MRA 排查) → "CT 和 MRI 区别/脑溢血早期症状/青光眼症状"
- **血糖偏高** (AI: 立刻打胰岛素, 立即开始低碳水) → "胰岛素打哪里/低碳水吃啥/血糖监测要啥仪器"
- **结果**: 10/10 clean (即使 AI reply 用"立即/立刻/必须"命令词, prompt 仍引导 LLM 输出"操作细节问句")

## 总计 6 轮 60 次 API 测试, 0/60 action suggestion ✅

### 覆盖维度
- **5 种 AI reply 风格**: 普通 / action-heavy / 极短 / 极长 / 满屏祈使 / medical claims
- **4 种 user input**: 下午很困 / 睡不好 / 运动伤膝 / 血压偏高 / 胸闷 / 头痛 / 血糖偏高

### 输出风格分布
- 90% 输出是"操作细节问句" (打哪里/怎么含服/具体吃啥)
- 10% 输出是"探索性问句" (盐 5g 多少勺/咖啡因代谢要多久/午后困和午饭有关吗)

## 代码改动详情 (ChatOverlay.swift fetchSuggestions 函数)

### 旧 prompt (line 996-1026)
```
你是健康助手的「下一步意图预测器」。基于用户的最近输入和 AI 的最新回复,预测用户接下来**最可能输入**或**想继续探索**的 2-3 条话题。

【判断依据】
\(userContext)AI 最新回复:\(response)

【预测方向】(三类话题混合输出 2-3 条)或者其他方向也可以
1. **追问细节**:用户想继续追问 AI 提到的某个点(贴合 AI 给的具体内容)
2. **立刻执行**:用户想马上执行的动作解析(贴合 AI 给的实操建议)   ← 删
3. **继续探索**:用户想继续探索的相关方向

【输出格式】
- 直接写出用户会打的字
- 单条 ≤ 18 字
- 严禁问号、严禁"试试"...
- 必须是用户**会输入**的具体短句

硬性规则:
1. 只输出 2-3 条
2. 严禁"建议"、"试试建议"等含"建议"的词
3. 严禁"某动作"、"某个"、"具体"等泛化占位词
4. 不带序号、注释、说明文字
```

### 新 prompt (line 996-1037)
```
你是健康助手的「下一步意图预测器」。基于用户的最近输入和 AI 的最新回复,预测用户接下来**最可能继续打字问**的 2-3 条话题。

【判断依据】
\(userContext)AI 最新回复:\(response)

【预测方向】(两类话题混合输出 2-3 条)
1. **追问细节**:用户想继续追问 AI 提到的某个具体点
2. **继续探索**:用户想继续探索的相关方向

【不要做】   ← 新增
- **不要给"行动建议"**!用户不缺行动建议,AI 上一条已经给了。
  **不要输出"试试 X"、"喝杯 X"、"起身 X"、"做 X 分钟"这种用户会去执行的动作**
- 不要输出抽象话题标签(如"健康"、"睡眠"),要具体
- 不要问号结尾的疑问句(用陈述句口吻)

【输出格式】
- 直接写出用户会继续打的字,模拟用户口吻
- 单条 ≤ 18 字
- 必须是用户**会主动输入**的具体短句

【风格示例】(好)
- 午餐后血糖怎么测
- 晒太阳要避开紫外线吗
- 咖啡喝多了会怎样

【风格示例】(❌ 不好 — 这种是 action suggestion, 严禁)   ← 新增
- 站起来走一走
- 试试喝杯咖啡
- 做一组深蹲

硬性规则:
1. 只输出 2-3 条,每条独占一行
2. 严禁"试试"、"建议"、"起身"、"喝杯"、"做一组"、"不妨"等动作或建议词
3. 严禁问号开头(但陈述句末尾可有"吗"表示询问口吻,如"怎么测"、"会怎样")
4. 不带序号、注释、说明文字
```

## 最终 prompt (完整版)

```swift
let prompt = """
你是健康助手的「下一步意图预测器」。基于用户的最近输入和 AI 的最新回复,预测用户接下来**最可能继续打字问**的 2-3 条话题。

【判断依据】
\(userContext.isEmpty ? "" : "\(userContext)")AI 最新回复:
\(response)

【预测方向】(两类话题混合输出 2-3 条)
1. **追问细节**:用户想继续追问 AI 提到的某个具体点(贴合 AI 提到的内容)
2. **继续探索**:用户想继续探索的相关方向(结合用户画像和上下文,可以稍微发散到相邻话题)

【不要做】
- **不要给"行动建议"**!用户不缺行动建议,AI 上一条已经给了。**不要输出"试试 X"、"喝杯 X"、"起身 X"、"做 X 分钟"这种用户会去执行的动作**
- 不要输出抽象话题标签(如"健康"、"睡眠"),要具体
- 不要问号结尾的疑问句(用陈述句口吻)

【输出格式】
- 直接写出用户会继续打的字,模拟用户口吻
- 单条 ≤ 18 字
- 必须是用户**会主动输入**的具体短句

【风格示例】(好)
- 午餐后血糖怎么测
- 晒太阳要避开紫外线吗
- 咖啡喝多了会怎样

【风格示例】(❌ 不好 — 这种是 action suggestion, 严禁)
- 站起来走一走
- 试试喝杯咖啡
- 做一组深蹲

硬性规则:
1. 只输出 2-3 条,每条独占一行
2. 严禁"试试"、"建议"、"起身"、"喝杯"、"做一组"、"不妨"等动作或建议词
3. 严禁问号开头(但陈述句末尾可有"吗"表示询问口吻,如"怎么测"、"会怎样")
4. 不带序号、注释、说明文字
"""
```

## Commit 历史 (worktree: fix/suggestion-prompt)

```
53aae0c test(suggestion-prompt): Round 4-5 stress test (20 runs, all clean)
becd205 fix(ChatOverlay): 推荐主题 prompt 调优 - 删'立刻执行'方向 + 加'不要行动建议'约束
```

## 文件清单
- `ios/Stick/Views/ChatOverlay.swift` (改 prompt, +24/-16)
- `PROMPT-ITERATION-LOG.md` (详细测试记录)
- `test-suggest.sh` / `test-round1.sh` / `test-round2.sh` / `test-round3.sh` / `test-round4.sh` / `test-round5.sh` (curl 测试脚本, 可复现)
- `SUGGESTION-PROMPT-REPORT.md` (本报告)

## 留下的 worktree
- 分支: `fix/suggestion-prompt`
- 位置: `/Users/horse/work/stick/ios/.claude/worktrees/suggestion-prompt`
- 未 merge, 等 user 决定

## Cron job
- ID: `e703d1b0`
- 每 10 分钟触发 (session-only, 7 天后自动过期)
- 用 `CronDelete` 可提前取消
